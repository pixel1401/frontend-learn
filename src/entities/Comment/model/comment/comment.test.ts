// import { CommentActions, CommentReducer } from './commentSlice';
import { CommentSchema } from '../types/comment';

describe('CommentSlice test12', () => {
    // const state : DeepPartial<CommentSchema> = {

    // };
    test('example', () => {
        // expect(CommentReducer(state as CommentSchema, CommentActions.increment())).toEqual({ value: 11 });
    });
});
