import { <FTName | capitalize>Actions, <FTName | capitalize>Reducer } from './<FTName>Slice';
import { <FTName | capitalize>Schema } from '../types/<FTName>Schema';

describe('<FTName | capitalize>Slice test', () => {
    const state : DeepPartial<<FTName | capitalize>Schema> = {
        
    };
    test('example', () => {
        // expect(<FTName | capitalize>Reducer(state as <FTName | capitalize>Schema, <FTName | capitalize>Actions.increment())).toEqual({ value: 11 });
    });
});
